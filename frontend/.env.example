# Copy to ``frontend/.env`` and adjust. Values prefixed VITE_ are exposed to the browser bundle.

# Optional API base URL (baked in at build time). Omit on Vercel to use same-origin /api rewrites.
# Local dev defaults to http://127.0.0.1:8000. Split-host example:
# VITE_BACKEND_ORIGIN=https://your-api.example.com

# Google Maps (Places autocomplete)
# VITE_GOOGLE_MAPS_API_KEY=
